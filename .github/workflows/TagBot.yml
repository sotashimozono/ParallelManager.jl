name: TagBot
on:
  schedule:
    - cron: "17 4 * * *"   # daily self-backfill of any registered-but-untagged version
  issue_comment:
    types: [created]        # JuliaTagBot app trigger
  workflow_dispatch:        # manual catch-up
jobs:
  tagbot:
    uses: QAtlasHub/.github/.github/workflows/tagbot.yml@main
    secrets: inherit
