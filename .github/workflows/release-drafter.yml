name: Release Drafter
on: { push: { branches: [main] }, pull_request: { types: [opened, reopened, synchronize, edited] } }
jobs: { draft: { uses: QAtlasHub/.github/.github/workflows/release-drafter.yml@main, secrets: inherit } }
